<div class="mt-3">
  <%= link_to "✏️ Editar", edit_evento_path(@evento), class: "btn btn-warning" %>

  <%= link_to "📄 Contrato", contrato_evento_path(@evento), class: "btn btn-primary" %>

  <%= link_to "⬅ Voltar", eventos_path, class: "btn btn-secondary" %>

  <%= button_to "🗑 Excluir", @evento,
      method: :delete,
      class: "btn btn-danger" %>
</div>